---
创建日期: YYYY-MM-DD
更新日期: YYYY-MM-DD
笔记类型: 复习题
知识点:
关联知识: []
review_kind: atomic-mirror # atomic-mirror or permitted extra kind
primary_atomic: atomic-xxx # required only for atomic-mirror
related_atomics: [] # must remain empty for atomic-mirror
task_units: [] # extra requires related_atomics or task_units
tags: []
---

# 规范核心对象 - 测试目标复习

## 题库类型与关联

- `atomic-mirror`：`primary_atomic` 对应恰好一个原子知识；`related_atomics` 必须为空。
- 额外复习只能使用 `boundary-comparison`、`error-diagnosis`、`integration-transfer`、`task-performance` 或 `retention`，并填写 `related_atomics` 或 `task_units`。
- 题目、预期检查和空白记录都不是实际作答，也不会激活复习队列。

## 因果预测

### 问题

给出运行前状态或代码变化，先写预测，再运行验证。

### 核对边界

- 必须说明导致结果的对象、状态、查找或调用顺序。

## 修错定位

### 问题

给出一个真实失败，定位首次偏离预期的位置并提出最小修正。

### 核对边界

- 必须包含失败机制、修正和实际结果。

## 换情境迁移

### 问题

在不同名称、数据或业务情境中重新使用同一机制，不复刻原示例。

### 核对边界

- 必须有独立实现和可观察输出或自动检查。

## 复习记录

- 尚无作答记录。
